@{
    ViewBag.Title = "Home Page";
}

@*
<!-- Top Products-->
<div id="carouselExampleFade" class="carousel slide carousel-fade mt-5" data-bs-ride="carousel">
    <div class="carousel-inner">
        <div class="carousel-item active">
            <div class="row w-75" style="margin:0 auto;">
                @for (int i = 0; i < 3; i++)
                {

                    var imgsrc = String.Format("data:image/gif;base64,{0}", ViewBag.List[i].ImageFileContent != null ? ViewBag.List[i].ImageFileContent : " ");

                    <div class="col-2 gx-5" style="text-align:center">
                        <div class="row">
                            <div class="col">
                                <img style="height: 200px;width:200px;" src="@imgsrc" class="img-thumbnail" />
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
        <div class="carousel-item">
            <div class="row w-75" style="margin:0 auto;">
                @for (int i = 3; i < 5; i++)
                {

                    var imgsrc = String.Format("data:image/gif;base64,{0}", ViewBag.List[i].ImageFileContent != null ? ViewBag.List[i].ImageFileContent : " ");
                    <div class="col-2 gx-5" style="text-align:center">
                        <div class="row">
                            <div class="col">
                                <img style="height: 200px;width:200px;" src="@imgsrc" class="img-thumbnail" />
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
        @*<div class="carousel-item">

            </div>*
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleFade" data-bs-slide="prev">
        <i class="fas fa-angle-left fa-3x" style="color:grey"></i>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleFade" data-bs-slide="next">
        <i class="fas fa-angle-right fa-3x" style="color:grey"></i>
        <span class="visually-hidden">Next</span>
    </button>
</div>
<div class="container mt-5 shadow rounded-2 bg-light">
    <div class="row justify-content-center">
        <div class="col-10 m-2">
            <h4 class="pt-2 pb-2">New Categories</h4>
            <div class="row pb-4">
                @for (int i = 0; i < 5; i++)
                {

                    var imgsrc = String.Format("data:image/gif;base64,{0}", ViewBag.List[i].ImageFileContent != null ? ViewBag.List[i].ImageFileContent : " ");
                    <div class="col-2 gx-5" style="text-align:center">
                        <img style="height: 150px;width:150px;" src="@imgsrc" class="img-thumbnail p-3 bg-white border-1 rounded shadow" />
                    </div>
                }
            </div>

        </div>
    </div>

</div>
<div class="container mt-5 shadow rounded-2 bg-light">
    <div class="row justify-content-center">
        <div class="col-10 m-2">
            <h4 class="pt-2 pb-2">New Brands</h4>
            <div class="row pb-4">
                @for (int i = 0; i < 5; i++)
                {

                    var imgsrc = String.Format("data:image/gif;base64,{0}", ViewBag.List[i].ImageFileContent != null ? ViewBag.List[i].ImageFileContent : " ");
                    <div class="col-2 gx-5" style="text-align:center">
                        <img style="height: 150px;width:150px;" src="@imgsrc" class="img-thumbnail p-3 bg-white border-1 rounded shadow" />
                    </div>
                }
            </div>

        </div>
    </div>

</div>
*@